version: "3"
services:
  react-docker-app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./src/:/app/src:ro
      - ./public:/app/public:ro
    # environment:
    #   - REACT_APP_NAME=brandongs
    env_file:
      - ./.env.dev
